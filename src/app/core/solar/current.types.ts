export interface current{
    x: Date,
    y: number
    fillColor?: string
}

export interface ICurrent{
    current: current;
    current2?: current;
    current3?: current;
    current_n?: current;
}

